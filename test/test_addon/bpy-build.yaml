addon_folder: MCprep_addon

install_versions:
  - 3.5+
  - 3.0..3.5
  - 2.8

output_name: MCprep_addon

build_actions:
  default: 
    script: "default.py"
  dev: 
    script: "dev.py"
    ignore_filters: 
      - "*.blend"
  depend_dev:
    depends_on:
      - dev
  dev_subaction:
    script: "old.py"
    subactions:
      - dev
  old:
    script: "old.py"
  no-script:
    ignore_filters:
      - "*.blend"
