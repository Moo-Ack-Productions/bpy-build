addon_folder: MCprep_addon
build_name: MCprep_addon

install_versions:
  - 3.5
  - 3.4

build_actions:
  default: 
    script: "default.py"
  dev: 
    script: "dev.py"
    ignore_filters: 
      - "*.blend"
